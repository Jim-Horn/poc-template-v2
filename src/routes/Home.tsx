import Start from '../components/Start';
import PageTransition from '../components/PageTransition';

const Home = () => (
  <PageTransition>
    <Start />
  </PageTransition>
);

export default Home;
